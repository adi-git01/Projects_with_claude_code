# Core dependencies (required)
pandas>=2.0.0
schedule>=1.2.0
requests>=2.31.0
python-dotenv>=1.0.0
colorama>=0.4.6
tabulate>=0.9.0

# Optional: yfinance (recommended but system will fallback to direct API if not available)
# yfinance>=0.2.40
# If yfinance installation fails, the system will use direct Yahoo Finance API calls
